# The headless CMS model is already understood

The learner correctly expected Payload to expose CMS content through an API
that the public ATF site consumes. The knowledge gap is narrower: publishing
changes document visibility, while browser-runtime, build-time, server-runtime,
or hybrid reads determine when visitors receive that published state. Future
lessons should start at this delivery-strategy fork rather than reteaching APIs.
